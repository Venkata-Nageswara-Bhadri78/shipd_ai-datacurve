import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

/*
REMEMBER:
- Your tests should be comprehensive. They should test 
  - edge cases 
  - large inputs 
  - as many scenarios as possible.
- Minimum 5 tests. Maximum 10 tests.
- More tests, higher-quality tests === higher payouts.
*/

// Default (package-private) access modifier for top-level class
class {entrypoint}Test {
    private final {entrypoint} solution = new {entrypoint}();

    @Test
    public void testOne() {
        // replace the function name with something descriptive. E.g. testBasicCase
        // Example test:
        // assertEquals(expected, solution.method(args));
    }

    @Test
    public void testTwo() {
        // replace the function name with something descriptive. E.g. testEdgeCase
        // Example test:
        // assertEquals(expected, solution.method(args));
    }

    @Test
    public void testThree() {
        // replace the function name with something descriptive. E.g. testLargeInput
        // Example test:
        // assertEquals(expected, solution.method(args));
    }

    @Test
    public void testFour() {

        // Example test:
        // assertEquals(expected, solution.method(args));
    }

    @Test
    public void testFive() {
        // Example test:
        // assertThrows(IllegalArgumentException.class, () -> {
        //     solution.method(invalidArgs);
        // });
    }
} 