#include <gtest/gtest.h>
#include "solution.cpp"

/*
REMEMBER:
- Your tests should be comprehensive. They should test 
  - edge cases 
  - large inputs 
  - as many scenarios as possible.
- Minimum 5 tests. Maximum 10 tests.
- More tests, higher-quality tests === higher payouts.
*/

TEST({entrypoint}Test, TestOne) {
    {entrypoint} solution;
    // Example test:
    // EXPECT_EQ(solution.method(args), expected);
}

TEST({entrypoint}Test, TestTwo) {
    {entrypoint} solution;
    // Example test:
    // EXPECT_EQ(solution.method(args), expected);
}

TEST({entrypoint}Test, TestThree) {
    {entrypoint} solution;
    // Example test:
    // EXPECT_EQ(solution.method(args), expected);
}

TEST({entrypoint}Test, TestFour) {
    {entrypoint} solution;
    // Example test:
    // EXPECT_EQ(solution.method(args), expected);
}

TEST({entrypoint}Test, TestFive) {
    {entrypoint} solution;
    // Example test:
    // EXPECT_THROW(solution.method(invalidArgs), std::invalid_argument);
} 